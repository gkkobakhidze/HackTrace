import { EnvOptionsType, SignerType, GroupDTO } from '../types';
export interface RemoveAdminsFromGroupType extends EnvOptionsType {
    chatId: string;
    admins: Array<string>;
    account?: string | null;
    signer?: SignerType | null;
    pgpPrivateKey?: string | null;
}
/**
 * Update Group information
 */
export declare const removeAdmins: (options: RemoveAdminsFromGroupType) => Promise<GroupDTO>;
