import { ENCRYPTION_TYPE, ENV } from '../constants';
import { SignerType, ProgressHookType } from '../types';
export declare class Encryption {
    private account;
    private decryptedPgpPvtKey;
    private pgpPublicKey;
    private env;
    private signer;
    private progressHook?;
    private userInstance;
    constructor(account: string, decryptedPgpPvtKey: string, pgpPublicKey: string, env: ENV, signer: SignerType, progressHook?: ((progress: ProgressHookType) => void) | undefined);
    info(): Promise<{
        decryptedPassword?: string | undefined;
        decryptedPgpPrivateKey: string;
        pgpPublicKey: string;
    }>;
    update(updatedEncryptionType: ENCRYPTION_TYPE, options?: {
        versionMeta?: {
            NFTPGP_V1?: {
                password: string;
            };
        };
    }): Promise<import("../types").IUser>;
}
