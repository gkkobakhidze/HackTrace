import { SignerType } from '../types';
export declare const signMessage: (signer: SignerType, message: string) => Promise<string>;
export declare const signTypedData: (signer: SignerType, domain: any, types: any, value: any, primaryType: string) => Promise<string>;
export declare const getAddress: (signer: SignerType) => Promise<string>;
